从1开始，not 0
vals <- seq(2,12, by = 2)
## [1] 2 4 6 8 10 12 

vals[c(1,3,5)]
## [1] 

crazymakers <- delay[delay>300]
            <- crazymakers[!is.na(crazymakers)]
            
or 是 |

sample(x, size, replace = FALSE, prob = NULL)

两长度不一样的序列 相加要小心

names(air) # names of data frame

is.matrix

mylist$stuff  #表里的列/元素

cbind() 是 column combind

NA can hold a place but NULL cannot.

str(air) structure(air)

mat[c(2,4), 5] = (row2,col5)&(row4,col5)
